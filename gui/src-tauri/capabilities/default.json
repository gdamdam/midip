{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the midip main window. The GUI talks to the engine only through our own typed commands, so no filesystem/shell plugins are granted.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:app:default",
    "core:app:allow-version"
  ]
}
